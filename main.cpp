#include<bits/stdc++.h>
using namespace std;

int main()
{
    string text = "Hello Sister";
    string add = "!";
    // cout << text << endl;
    // cout << text.size() << endl;
    // cout << text.max_size() << endl; 
    // cout << text.capacity() << endl; 
    // text.clear();
    // text.resize(16, 'r');
    // cout << text[0] << endl; 
    // cout << text.at(0) << endl; 
    // cout << text.back() << endl; 
    // cout << text.front() << endl;
    // text += add;
    // text.append(add);
    // text.push_back('A');
    // text.pop_back();

    cout << text << endl; 

    return 0;
} 