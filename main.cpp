#include<bits/stdc++.h>
using namespace std;

int main()
{
    string text = "Hello Sister";
    
    for (auto it = text.begin(); it < text.end(); it++) {
        cout << *it;
    }

    return 0;
} 